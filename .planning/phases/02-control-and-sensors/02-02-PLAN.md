---
phase: 02-control-and-sensors
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - custom_components/dreame_mower/sensor.py
  - custom_components/dreame_mower/button.py
  - custom_components/dreame_mower/switch.py
  - custom_components/dreame_mower/select.py
  - custom_components/dreame_mower/coordinator.py
  - custom_components/dreame_mower/const.py
autonomous: false
requirements: [SENS-01, SENS-03, SENS-05, SENS-06, SENS-07, SENS-08, CLEAN-01]

must_haves:
  truths:
    - "No vacuum consumable sensors appear (side brush, filter, tank filter, silver ion, lens brush, squeegee)"
    - "No vacuum reset buttons appear (reset side brush, reset filter, etc.)"
    - "No camera/vacuum switches appear (AI obstacle image, pet detection, stain avoidance, etc.)"
    - "No floor material segment selects appear"
    - "Battery, error, progress, area, blade sensors still exist and function"
    - "Blade reset button still exists and functions"
    - "Coordinator only checks blade consumable wear, not vacuum consumables"
  artifacts:
    - path: "custom_components/dreame_mower/sensor.py"
      provides: "Mower-only sensor descriptions"
      contains: "BATTERY_LEVEL"
    - path: "custom_components/dreame_mower/button.py"
      provides: "Mower-only button descriptions"
      contains: "RESET_BLADES"
    - path: "custom_components/dreame_mower/switch.py"
      provides: "Mower-only switch descriptions"
    - path: "custom_components/dreame_mower/select.py"
      provides: "Mower-only select descriptions"
    - path: "custom_components/dreame_mower/coordinator.py"
      provides: "Blade-only consumable checks"
    - path: "custom_components/dreame_mower/const.py"
      provides: "Mower-only constants"
      contains: "CONSUMABLE_BLADES"
  key_links:
    - from: "custom_components/dreame_mower/coordinator.py"
      to: "custom_components/dreame_mower/const.py"
      via: "CONSUMABLE_BLADES import"
      pattern: "CONSUMABLE_BLADES"
    - from: "custom_components/dreame_mower/sensor.py"
      to: "DreameMowerProperty.BATTERY_LEVEL"
      via: "sensor description property_key"
      pattern: "BATTERY_LEVEL"
---

<objective>
Remove all vacuum-specific entity descriptions from sensor, button, switch, select, coordinator, and const files so only mower-relevant entities are exposed.

Purpose: Eliminate ghost vacuum entities (side brush, filter, tank filter, silver ion, squeegee, lens brush, camera features) that should never appear for a mower device. Existing mower sensors (battery, state, error, progress, blade wear) remain intact.
Output: Clean entity platform files with only mower-relevant descriptions, coordinator checking only blade consumable, and const.py without vacuum constants.
</objective>

<execution_context>
@/Users/nilshoffmann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nilshoffmann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-control-and-sensors/02-RESEARCH.md
@.planning/phases/02-control-and-sensors/02-01-SUMMARY.md

CRITICAL: Source code is at /Volumes/config/custom_components/dreame_mower/
Git repo is at /Volumes/config/custom_components/dreame_mower_repo/
Changes must be made in the LIVE directory first, then synced to the git repo.
The existing dreame_vacuum integration (for vacuum "Momo") must NOT be touched.
</context>

<interfaces>
<!-- Entities to KEEP vs REMOVE. Extracted from codebase analysis. -->

sensor.py SENSORS tuple — KEEP these descriptions:
- CLEANING_TIME (both standard and "Mapping Time" variant)
- CLEANED_AREA (SENS-06)
- STATE (SENS-02 — state sensor)
- STATUS
- RELOCATION_STATUS
- TASK_TYPE
- ERROR (SENS-03)
- CHARGING_STATUS
- BATTERY_LEVEL (SENS-01)
- BLADES_LEFT (SENS-07)
- BLADES_TIME_LEFT (SENS-07)
- FIRST_CLEANING_DATE
- TOTAL_CLEANING_TIME
- CLEANING_COUNT
- TOTAL_CLEANED_AREA
- current_zone
- cleaning_history
- cruising_history
- CLEANING_PROGRESS (SENS-05)
- firmware_version

sensor.py SENSORS tuple — REMOVE these descriptions:
- STREAM_STATUS (camera streaming — no camera on Mova 600 Plus)
- SIDE_BRUSH_LEFT (vacuum)
- SIDE_BRUSH_TIME_LEFT (vacuum)
- FILTER_LEFT (vacuum)
- FILTER_TIME_LEFT (vacuum)
- SENSOR_DIRTY_LEFT (vacuum — leave exists_fn guarded but remove per research)
- SENSOR_DIRTY_TIME_LEFT (vacuum — same)
- TANK_FILTER_LEFT (vacuum)
- TANK_FILTER_TIME_LEFT (vacuum)
- SILVER_ION_LEFT (vacuum)
- SILVER_ION_TIME_LEFT (vacuum)
- LENSBRUSH_LEFT (vacuum)
- LENSBRUSH_TIME_LEFT (vacuum)
- SQUEEGEE_LEFT (vacuum)
- SQUEEGEE_TIME_LEFT (vacuum)

button.py BUTTONS tuple — KEEP:
- RESET_BLADES (SENS-08)
- CLEAR_WARNING
- start_fast_mapping
- start_mapping

button.py BUTTONS tuple — REMOVE:
- RESET_SIDE_BRUSH
- RESET_FILTER
- RESET_SENSOR
- RESET_SILVER_ION
- RESET_LENSBRUSH
- RESET_SQUEEGEE
- RESET_TANK_FILTER (not present in code — confirmed only 7 vacuum resets listed)

switch.py SWITCHES tuple — REMOVE:
- AI_OBSTACLE_IMAGE_UPLOAD
- AI_OBSTACLE_PICTURE
- AI_PET_DETECTION
- AI_HUMAN_DETECTION
- FUZZY_OBSTACLE_DETECTION
- AI_PET_AVOIDANCE
- PET_PICTURE
- PET_FOCUSED_DETECTION
- STAIN_AVOIDANCE
- FLOOR_DIRECTION_CLEANING
- PET_FOCUSED_CLEANING
- STREAMING_VOICE_PROMPT
- camera_light_brightness_auto

select.py SEGMENT_SELECTS — REMOVE:
- floor_material
- floor_material_direction

coordinator.py _check_consumables — REMOVE calls for:
- CONSUMABLE_SIDE_BRUSH / NOTIFICATION_ID_REPLACE_SIDE_BRUSH / SIDE_BRUSH_LEFT
- CONSUMABLE_FILTER / NOTIFICATION_ID_REPLACE_FILTER / FILTER_LEFT
- CONSUMABLE_TANK_FILTER / NOTIFICATION_ID_REPLACE_TANK_FILTER / TANK_FILTER_LEFT
- CONSUMABLE_SENSOR / NOTIFICATION_ID_CLEAN_SENSOR / SENSOR_DIRTY_LEFT
- CONSUMABLE_SQUEEGEE / NOTIFICATION_ID_REPLACE_SQUEEGEE / SQUEEGEE_LEFT
- CONSUMABLE_LENSBRUSH / NOTIFICATION_ID_REPLACE_LENSBRUSH / LENSBRUSH_LEFT

const.py — REMOVE:
- CONSUMABLE_SIDE_BRUSH through CONSUMABLE_SQUEEGEE (keep CONSUMABLE_BLADES)
- NOTIFICATION_ID_REPLACE_SIDE_BRUSH, _FILTER, _TANK_FILTER, _CLEAN_SENSOR, _REPLACE_LENSBRUSH, _REPLACE_SQUEEGEE
- FAN_SPEED_SILENT, _STANDARD, _STRONG, _TURBO
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove vacuum entity descriptions from sensor.py, button.py, switch.py, select.py</name>
  <files>
    /Volumes/config/custom_components/dreame_mower/sensor.py
    /Volumes/config/custom_components/dreame_mower/button.py
    /Volumes/config/custom_components/dreame_mower/switch.py
    /Volumes/config/custom_components/dreame_mower/select.py
  </files>
  <action>
Read each file fully before modifying. For each file, remove the vacuum-only entity descriptions from the tuples. Do NOT modify any class definitions, helper functions, or async_setup_entry functions. Only remove entries from the tuple definitions.

**sensor.py — Remove 15 descriptions from SENSORS tuple:**
Remove the entire `DreameMowerSensorEntityDescription(...)` blocks for:
STREAM_STATUS, SIDE_BRUSH_LEFT, SIDE_BRUSH_TIME_LEFT, FILTER_LEFT, FILTER_TIME_LEFT, SENSOR_DIRTY_LEFT, SENSOR_DIRTY_TIME_LEFT, TANK_FILTER_LEFT, TANK_FILTER_TIME_LEFT, SILVER_ION_LEFT, SILVER_ION_TIME_LEFT, LENSBRUSH_LEFT, LENSBRUSH_TIME_LEFT, SQUEEGEE_LEFT, SQUEEGEE_TIME_LEFT.

Also remove the now-unused imports: `DreameMowerStreamStatus` from the `from .dreame import` block, and `STREAM_STATUS_TO_ICON` dict (lines 42-47) since STREAM_STATUS sensor is removed.

Add comment at top of SENSORS tuple: `# FORK: CLEAN-01 - vacuum-only sensor descriptions removed (side brush, filter, tank filter, silver ion, lensbrush, squeegee, sensor dirty, stream status)`

Keep all other sensor descriptions (battery, state, error, cleaned_area, cleaning_progress, blades, etc.).

**button.py — Remove 6 descriptions from BUTTONS tuple:**
Remove `DreameMowerButtonEntityDescription(...)` blocks for:
RESET_SIDE_BRUSH, RESET_FILTER, RESET_SENSOR, RESET_SILVER_ION, RESET_LENSBRUSH, RESET_SQUEEGEE.

Add comment: `# FORK: CLEAN-01 - vacuum-only reset button descriptions removed`

Keep RESET_BLADES (SENS-08), CLEAR_WARNING, start_fast_mapping, start_mapping.

**switch.py — Remove 13 descriptions from SWITCHES tuple:**
Remove `DreameMowerSwitchEntityDescription(...)` blocks for:
AI_OBSTACLE_IMAGE_UPLOAD, AI_OBSTACLE_PICTURE, AI_PET_DETECTION, AI_HUMAN_DETECTION, FUZZY_OBSTACLE_DETECTION, AI_PET_AVOIDANCE, PET_PICTURE, PET_FOCUSED_DETECTION, STAIN_AVOIDANCE, FLOOR_DIRECTION_CLEANING, PET_FOCUSED_CLEANING, STREAMING_VOICE_PROMPT, camera_light_brightness_auto.

Also clean up now-unused imports if any of `DreameMowerStrAIProperty`, `DreameMowerAIProperty` are no longer referenced. Check: `AI_OBSTACLE_DETECTION` uses `DreameMowerAIProperty` and is being KEPT (it is listed as a valid mower feature for obstacle detection via LiDAR). `FILL_LIGHT` uses `DreameMowerAutoSwitchProperty` and is kept. `COLLISION_AVOIDANCE` uses `DreameMowerAutoSwitchProperty` and is kept. So keep all imports — some AI properties are still used by AI_OBSTACLE_DETECTION.

However, `DreameMowerStrAIProperty` is ONLY used by `AI_HUMAN_DETECTION` which is being removed. Remove `DreameMowerStrAIProperty` from the import.

Add comment: `# FORK: CLEAN-01 - vacuum/camera-only switch descriptions removed`

**select.py — Remove 2 descriptions from SEGMENT_SELECTS tuple:**
Remove `DreameMowerSelectEntityDescription(...)` blocks for:
floor_material, floor_material_direction.

Also clean up now-unused imports: `DreameMowerFloorMaterial`, `DreameMowerFloorMaterialDirection`, `FLOOR_MATERIAL_CODE_TO_NAME`, `FLOOR_MATERIAL_DIRECTION_CODE_TO_NAME`. Remove the `FLOOR_MATERIAL_TO_ICON` and `FLOOR_MATERIAL_DIRECTION_TO_ICON` dicts.

Add comment: `# FORK: CLEAN-01 - vacuum floor material segment selects removed`

After all changes, verify each file passes syntax check.
  </action>
  <verify>
    <automated>python3 -c "
import ast
files = [
    '/Volumes/config/custom_components/dreame_mower/sensor.py',
    '/Volumes/config/custom_components/dreame_mower/button.py',
    '/Volumes/config/custom_components/dreame_mower/switch.py',
    '/Volumes/config/custom_components/dreame_mower/select.py',
]
for f in files:
    ast.parse(open(f).read())
    print(f'{f.split(\"/\")[-1]}: syntax OK')

# Verify vacuum entities removed
sensor = open(files[0]).read()
assert 'SIDE_BRUSH_LEFT' not in sensor, 'side brush sensor still present'
assert 'FILTER_LEFT' not in sensor, 'filter sensor still present'
assert 'SQUEEGEE_LEFT' not in sensor, 'squeegee sensor still present'
assert 'STREAM_STATUS' not in sensor, 'stream status sensor still present'
assert 'BATTERY_LEVEL' in sensor, 'battery sensor accidentally removed'
assert 'BLADES_LEFT' in sensor, 'blades sensor accidentally removed'
assert 'CLEANING_PROGRESS' in sensor, 'progress sensor accidentally removed'

button = open(files[1]).read()
assert 'RESET_SIDE_BRUSH' not in button, 'side brush reset still present'
assert 'RESET_FILTER' not in button, 'filter reset still present'
assert 'RESET_BLADES' in button, 'blades reset accidentally removed'

switch = open(files[2]).read()
assert 'AI_OBSTACLE_IMAGE_UPLOAD' not in switch, 'AI image upload switch still present'
assert 'PET_PICTURE' not in switch, 'pet picture switch still present'
assert 'STAIN_AVOIDANCE' not in switch, 'stain avoidance switch still present'

select = open(files[3]).read()
assert 'floor_material_direction' not in select, 'floor material direction select still present'

print('All vacuum entity removal assertions passed')
"</automated>
  </verify>
  <done>Vacuum-only descriptions removed from all 4 entity platform files. Mower-relevant entities (battery, state, error, progress, blade, blade reset) remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Prune vacuum consumable checks from coordinator.py and constants from const.py</name>
  <files>
    /Volumes/config/custom_components/dreame_mower/coordinator.py
    /Volumes/config/custom_components/dreame_mower/const.py
  </files>
  <action>
Read each file fully before modifying.

**coordinator.py — Prune _check_consumables method:**

The `_check_consumables` method (around line 226) currently calls `_check_consumable` for: BLADES, SIDE_BRUSH, FILTER, TANK_FILTER, SENSOR (conditional), SQUEEGEE, LENSBRUSH.

Keep ONLY the BLADES check. Remove all other `_check_consumable(...)` calls. The method should look like:

```python
def _check_consumables(self):
    # FORK: CLEAN-01 - only check blade consumable for mowers
    self._check_consumable(
        CONSUMABLE_BLADES,
        NOTIFICATION_ID_REPLACE_BLADES,
        DreameMowerProperty.BLADES_LEFT,
    )
```

Clean up coordinator.py imports from `from .const import (...)`:
- Remove: `CONSUMABLE_SIDE_BRUSH, CONSUMABLE_FILTER, CONSUMABLE_TANK_FILTER, CONSUMABLE_SENSOR, CONSUMABLE_LENSBRUSH, CONSUMABLE_SQUEEGEE`
- Remove: `NOTIFICATION_ID_REPLACE_SIDE_BRUSH, NOTIFICATION_ID_REPLACE_FILTER, NOTIFICATION_ID_REPLACE_TANK_FILTER, NOTIFICATION_ID_CLEAN_SENSOR, NOTIFICATION_ID_REPLACE_LENSBRUSH, NOTIFICATION_ID_REPLACE_SQUEEGEE`
- Also remove `NOTIFICATION_ID_SILVER_ION` if present (check the import block around line 48)
- Keep: `CONSUMABLE_BLADES, NOTIFICATION_ID_REPLACE_BLADES`

**const.py — Remove vacuum-only constants:**

Remove these constant definitions:
- `CONSUMABLE_SIDE_BRUSH = "side_brush"` (line ~137)
- `CONSUMABLE_FILTER = "filter"` (line ~138)
- `CONSUMABLE_TANK_FILTER = "tank_filter"` (line ~139)
- `CONSUMABLE_SENSOR = "sensor"` (line ~140)
- `CONSUMABLE_SILVER_ION = "silver_ion"` (line ~141)
- `CONSUMABLE_LENSBRUSH = "lensbrush"` (line ~142)
- `CONSUMABLE_SQUEEGEE = "squeegee"` (line ~143)

Keep: `CONSUMABLE_BLADES = "blades"`

Remove these notification constants:
- `NOTIFICATION_ID_REPLACE_SIDE_BRUSH`
- `NOTIFICATION_ID_REPLACE_FILTER`
- `NOTIFICATION_ID_REPLACE_TANK_FILTER`
- `NOTIFICATION_ID_CLEAN_SENSOR`
- `NOTIFICATION_ID_REPLACE_LENSBRUSH`
- `NOTIFICATION_ID_REPLACE_SQUEEGEE`

Keep: `NOTIFICATION_ID_REPLACE_BLADES`

Remove fan speed constants:
- `FAN_SPEED_SILENT`
- `FAN_SPEED_STANDARD`
- `FAN_SPEED_STRONG`
- `FAN_SPEED_TURBO`

Add comment above remaining CONSUMABLE_BLADES: `# FORK: CLEAN-01 - vacuum consumable constants removed (side brush, filter, tank filter, sensor, silver ion, lensbrush, squeegee)`
Add comment where FAN_SPEED was: `# FORK: CLEAN-01 - vacuum fan speed constants removed`

After changes, verify both files pass syntax check.
  </action>
  <verify>
    <automated>python3 -c "
import ast
files = [
    '/Volumes/config/custom_components/dreame_mower/coordinator.py',
    '/Volumes/config/custom_components/dreame_mower/const.py',
]
for f in files:
    ast.parse(open(f).read())
    print(f'{f.split(\"/\")[-1]}: syntax OK')

coord = open(files[0]).read()
assert 'CONSUMABLE_SIDE_BRUSH' not in coord, 'side brush consumable still in coordinator'
assert 'CONSUMABLE_FILTER' not in coord, 'filter consumable still in coordinator'
assert 'CONSUMABLE_BLADES' in coord, 'blades consumable accidentally removed from coordinator'

const = open(files[1]).read()
assert 'CONSUMABLE_SIDE_BRUSH' not in const, 'side brush const still present'
assert 'FAN_SPEED_SILENT' not in const, 'fan speed const still present'
assert 'CONSUMABLE_BLADES' in const, 'blades const accidentally removed'
assert 'NOTIFICATION_ID_REPLACE_BLADES' in const, 'blades notification accidentally removed'

print('All coordinator/const pruning assertions passed')
"</automated>
  </verify>
  <done>coordinator.py only checks blade consumable. const.py has no vacuum consumable constants, no vacuum notification IDs, no fan speed constants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all Phase 2 entities in Home Assistant</name>
  <files></files>
  <action>
Complete Phase 2 implementation has been applied across both plans:
- lawn_mower.py: RETURNING state mapping fixed, dock sends STOP then DOCK, vacuum services removed (Plan 01)
- binary_sensor.py: new connectivity binary sensor using device.device_connected (Plan 01)
- __init__.py: Platform.BINARY_SENSOR registered (Plan 01)
- sensor.py: 15 vacuum sensor descriptions removed, mower sensors intact (Plan 02)
- button.py: 6 vacuum reset buttons removed, blade reset intact (Plan 02)
- switch.py: 13 camera/vacuum switches removed (Plan 02)
- select.py: 2 floor material segment selects removed (Plan 02)
- coordinator.py: only blade consumable check remains (Plan 02)
- const.py: vacuum consumable/notification/fan-speed constants removed (Plan 02)

User must restart HA and verify entity list.
  </action>
  <verify>
    <automated>python3 -c "
# Cross-file import consistency check: verify no file imports a symbol removed from const.py
import ast
const = open('/Volumes/config/custom_components/dreame_mower/const.py').read()
lm = open('/Volumes/config/custom_components/dreame_mower/lawn_mower.py').read()
coord = open('/Volumes/config/custom_components/dreame_mower/coordinator.py').read()

removed = ['CONSUMABLE_SIDE_BRUSH', 'CONSUMABLE_FILTER', 'CONSUMABLE_TANK_FILTER',
           'CONSUMABLE_SENSOR', 'CONSUMABLE_SILVER_ION', 'CONSUMABLE_LENSBRUSH',
           'CONSUMABLE_SQUEEGEE', 'FAN_SPEED_SILENT', 'FAN_SPEED_STANDARD',
           'FAN_SPEED_STRONG', 'FAN_SPEED_TURBO', 'NOTIFICATION_ID_REPLACE_SIDE_BRUSH',
           'NOTIFICATION_ID_REPLACE_FILTER', 'NOTIFICATION_ID_REPLACE_TANK_FILTER',
           'NOTIFICATION_ID_CLEAN_SENSOR', 'NOTIFICATION_ID_REPLACE_LENSBRUSH',
           'NOTIFICATION_ID_REPLACE_SQUEEGEE']
for sym in removed:
    assert sym not in const, f'{sym} still defined in const.py'
    assert sym not in lm, f'{sym} still imported in lawn_mower.py'
    assert sym not in coord, f'{sym} still imported in coordinator.py'

# Verify all files still parse
for f in ['const.py', 'lawn_mower.py', 'coordinator.py', 'sensor.py', 'button.py',
          'switch.py', 'select.py', '__init__.py', 'binary_sensor.py']:
    path = f'/Volumes/config/custom_components/dreame_mower/{f}'
    ast.parse(open(path).read())

print('Cross-file consistency: all assertions passed, all files parse OK')
"</automated>
  </verify>
  <done>User confirms: correct entities visible in HA, no vacuum ghosts, state mapping shows correct activity, connectivity binary sensor works.</done>
</task>

</tasks>

<verification>
All 6 modified files + 1 new file parse without syntax errors. Vacuum-only entity descriptions, consumable checks, constants, and services are removed. Mower-relevant entities remain intact. Cross-file import consistency verified. Final human verification confirms correct entity list in HA.
</verification>

<success_criteria>
1. sensor.py has no vacuum consumable sensor descriptions (side brush, filter, tank filter, silver ion, squeegee, lens brush, sensor dirty, stream status)
2. sensor.py retains battery (SENS-01), state, error (SENS-03), cleaning progress (SENS-05), cleaned area (SENS-06), blades (SENS-07) sensors
3. button.py retains RESET_BLADES (SENS-08) and removes all vacuum reset buttons
4. switch.py has no camera/vacuum-specific switches
5. select.py has no floor material segment selects
6. coordinator.py _check_consumables only checks BLADES
7. const.py has no vacuum consumable, notification, or fan speed constants
8. User confirms correct entity list in HA after restart
</success_criteria>

<output>
After completion, create `.planning/phases/02-control-and-sensors/02-02-SUMMARY.md`
</output>
