---
phase: 01-stabilize-and-connect
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - /Volumes/config/custom_components/dreame_mower_repo/custom_components/dreame_mower/__init__.py
autonomous: false
requirements:
  - CONN-01
  - CONN-02
  - CONN-03

must_haves:
  truths:
    - "User can add Mova 600 Plus through config flow UI using MOVAhome credentials"
    - "Integration discovers the Mova 600 Plus and shows its model ID"
    - "Cloud MQTT connection is established and stays stable for at least 10 minutes"
    - "HA logs show no crashes, no deprecation warnings, no property errors during pairing"
  artifacts:
    - path: "custom_components/dreame_mower/__init__.py"
      provides: "FORK comment documenting debug logging instructions for initial pairing"
      contains: "FORK: Phase 1 pairing"
  key_links:
    - from: "config_flow.py"
      to: "dreame/protocol.py"
      via: "DreameMowerProtocol connects to MOVAhome cloud"
      pattern: "DreameMowerProtocol"
    - from: "dreame/protocol.py"
      to: "Mova 600 Plus device"
      via: "Cloud MQTT subscription receives property updates"
      pattern: "mqtt.*connect"
---

<objective>
Pair the real Mova 600 Plus device through the HA config flow and verify stable cloud connectivity.

Purpose: All bug fixes from Plan 01 must be validated against the real device. This is the first live test -- it will discover the Mova 600 Plus model ID, confirm MQTT connectivity, and reveal whether any additional unknown property IDs need handling.

Output: Successfully paired device with confirmed model ID and stable connection logged.
</objective>

<execution_context>
@/Users/nilshoffmann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nilshoffmann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilize-and-connect/01-RESEARCH.md
@.planning/phases/01-stabilize-and-connect/01-01-SUMMARY.md

<interfaces>
<!-- Config flow entry points for Mova account pairing -->

From config_flow.py:
- MOVAHOME constant = "Mova Account" (line 62) -- user selects this account type
- async_step_mova() method (line ~492) -- handles Mova credentials input
- async_step_mova_devices() (line ~549) -- discovers devices on account, uses device["model"] directly

From dreame/protocol.py:
- MOVA_STRINGS used when account_type is Mova (line 286)
- MQTT connect with cloud broker for device communication
- _on_client_connect, _on_client_disconnect, _on_client_message callbacks

From __init__.py:
- async_setup_entry() creates coordinator and forwards platforms
- LOGGER = logging.getLogger(__name__)

Key debug logging commands for HA:
- HA service: logger.set_level with {"custom_components.dreame_mower": "debug"}
- Or add to configuration.yaml logger section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable debug logging and restart HA</name>
  <files>
    /Volumes/config/custom_components/dreame_mower_repo/custom_components/dreame_mower/__init__.py
  </files>
  <action>
Before the live pairing test, ensure debug logging is active for the integration so all property IDs, model strings, and MQTT events are captured.

1. Add a temporary FORK comment near the top of `__init__.py` (after imports, before any function definitions) noting that debug logging should be enabled for initial pairing:

```python
# FORK: Phase 1 pairing - enable debug logging via HA logger service
# Run: ha service logger.set_level --data '{"custom_components.dreame_mower": "debug", "custom_components.dreame_mower.dreame": "debug"}'
```

2. Verify the integration can be loaded by HA by checking the `async_setup_entry` function exists and the manifest is loadable.

3. Restart Home Assistant to pick up the code changes from Plan 01:
   - Use HA CLI: `ha core restart` (if available)
   - Or use the HA MCP restart service
   - Wait for HA to fully start (check `ha core info` for running state)

4. After restart, enable debug logging via the HA service call:
   - Service: `logger.set_level`
   - Data: `{"custom_components.dreame_mower": "debug", "custom_components.dreame_mower.dreame": "debug"}`
  </action>
  <verify>
    <automated>cd /Volumes/config/custom_components/dreame_mower_repo && python3 -c "
with open('custom_components/dreame_mower/__init__.py') as f:
    src = f.read()
assert 'FORK: Phase 1 pairing' in src, 'Missing FORK debug logging comment in __init__.py'
print('__init__.py has FORK pairing comment')
"</automated>
  </verify>
  <done>FORK debug logging comment added to __init__.py, HA restarted with Plan 01 code changes active, debug logging enabled for dreame_mower component</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live Mova 600 Plus pairing and stable connection</name>
  <action>
Human verifies the full config flow pairing with the real Mova 600 Plus device and confirms stable MQTT connectivity. All Plan 01 bug fixes are validated against real device data.
  </action>
  <verify>User confirms pairing success and reports model ID, connection stability, and any log errors</verify>
  <done>Mova 600 Plus paired via config flow, model ID recorded, MQTT stable for 10+ minutes, no crashes in logs</done>
  <what-built>All bug fixes from Plan 01 are deployed and HA is running with debug logging. The integration is ready for live pairing with the Mova 600 Plus.</what-built>
  <how-to-verify>
    1. Open Home Assistant: Settings > Devices and Services > Add Integration
    2. Search for "Dreame Mower" and select it
    3. Choose "Mova Account" as the account type
    4. Enter your MOVAhome app email and password
    5. Wait for device discovery -- your Mova 600 Plus should appear in the list
    6. Select the Mova 600 Plus and complete setup
    7. After setup, verify:
       a. The device appears in Settings > Devices and Services > Dreame Mower
       b. Note the model ID shown in the device info (e.g., "mova.mower.xxxxx")
       c. Wait at least 10 minutes and check HA logs for:
          - No "ValueError" or "not a valid DreameMowerProperty" errors
          - No "TypeError: list indices" errors
          - No "AttributeError: _webrtc_provider" errors
          - Stable MQTT connection (no repeated disconnect/reconnect cycles)
       d. Open integration options (gear icon) -- should open without 500 error
    8. Report back:
       - The exact model ID string shown for the Mova 600 Plus
       - Any errors in the HA log (Settings > System > Logs)
       - Whether the device shows as "online" or "connected"
       - Any unknown property debug messages (search logs for "Unknown property ID")
  </how-to-verify>
  <resume-signal>Type "approved" with the model ID and any findings, or describe any errors encountered</resume-signal>
</task>

</tasks>

<verification>
This plan's verification is primarily the human checkpoint. The automated checks confirm code integrity before the human test:

```bash
cd /Volumes/config/custom_components/dreame_mower_repo

# Quick sanity check that all Python files parse
python3 -c "
import py_compile, glob
files = glob.glob('custom_components/dreame_mower/**/*.py', recursive=True)
for f in files:
    try:
        py_compile.compile(f, doraise=True)
    except py_compile.PyCompileError as e:
        print(f'SYNTAX ERROR: {e}')
        raise
print(f'All {len(files)} Python files parse OK')
"
```
</verification>

<success_criteria>
1. Mova 600 Plus appears in device discovery during config flow setup
2. Config flow completes without errors using MOVAhome credentials (CONN-01)
3. Device model ID is discovered and recorded (CONN-02)
4. MQTT connection remains stable for 10+ minutes without disconnect loops (CONN-03)
5. HA logs show no ValueError, TypeError, or AttributeError from the integration
6. Integration options page opens without 500 error
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize-and-connect/01-02-SUMMARY.md`

Include in summary:
- Discovered Mova 600 Plus model ID
- Any unknown property IDs logged during pairing
- Connection stability observations
- Any issues requiring follow-up
</output>